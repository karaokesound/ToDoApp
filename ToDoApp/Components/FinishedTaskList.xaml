<UserControl x:Class="ToDoApp.Components.FinishedTaskList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoApp.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <StackPanel>
                <TextBlock Text="{Binding TaskTitle}"
                                           Margin ="5"/>
                <TextBlock Text="{Binding TaskDescription}"
                                           Margin ="5"/>
                <TextBlock Text="{Binding TaskStartTime}"
                                           Margin ="5"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
